# Frontend build-time переменные (Vite читает только префикс VITE_).
# В dev пусто → фронт ходит на /api (проксируется на backend).
VITE_API_URL=
# Username бота (без @) — нужен для Telegram Login Widget в браузере.
VITE_BOT_USERNAME=askbrows_bot
